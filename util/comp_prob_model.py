from pdb import set_trace as bp
import numpy as np

#def get_normalized_data(data):
#    bp()
#    min_data = np.min(data)
#    max_data = np.max(data)
#    norm_data = (data - np.min(data)) / (np.max(cp_df.lat_dist) - np.min(cp_df.lat_dist))